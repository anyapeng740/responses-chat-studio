{
  "name": "responses-chat-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser chat UI for a custom OpenAI Responses relay with SSE streaming.",
  "scripts": {
    "dev": "node --watch server.mjs",
    "start": "node server.mjs",
    "check": "node --check server.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "markdown-it": "^14.1.1"
  }
}
